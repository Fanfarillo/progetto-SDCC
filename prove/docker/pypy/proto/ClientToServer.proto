syntax = "proto3";

package proto;

service Greeter {
    rpc GetServiceName(Request) returns (Response);
}

message Request {
    string id = 1;
    string name = 2;
}

message Response {
    string serviceName = 1;
}
